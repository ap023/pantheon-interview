# Working baseline — same as config/fleet_defaults.yaml. Diff any other
# file in this directory against this one to see exactly what's broken.
takt_s: 4.0
position_tolerance_rad: 0.05
control_gain_kp: 4.0
autoclear: false
calibration_max_age_s: 300.0
